<div class="container">
    <div class = "col-md-6 col-md-offset-3">
        <div class="text-center center-block"> 
            <h4>Upload Your Company's Transcript</h4><br>
            <div class="form-group">
                <input type="file" accept='text/plain' class="center-block" id="file_upload"><br>
                <button type="submit" class="btn btn-default" id="submit_file">Submit</button>
            </div>  
        </div>
    </div>
    <div class = "col-md-3">
        <button type="submit" class="btn btn-default btn-sm pull-right hide-code">Show Code</button>
    </div>
    <div class = "col-md-6 col-md-offset-3 trans_output">
    </div>
    <div class = "col-md-10 col-md-offset-1 toggle code_display">
        <pre>
            <xmp>
                HTML:
                <div class="container">
                    <button type="submit" class="pull-right hide-code">Show Code</button><br>
                    <div class="text-center center-block"> 
                        <h4>Upload Your Company's Transcript</h4><br>
                        <div class="form-group">
                            <input type="file" accept='text/plain' class="center-block" id="file_upload"><br>
                            <button type="submit" class="btn btn-default" id="submit_file">Submit</button>
                        </div>  
                    </div>
                    <div class = "col-md-6 col-md-offset-3 trans_output">
                    </div>
                    <div class = "col-md-10 col-md-offset-1 toggle">
                        <pre id="fileDisplayArea">
                                <!-- This place is where this showing code resides -->
                        </pre>
                    </div>
                </div>

                JAVASCRIPT:
                var fileInput = $('#file_upload')[0]; // or var fileInput = document.getElementById('fileInput');
                var fileDisplayArea = document.getElementById('fileDisplayArea');
                    $("#submit_file").click(function() {
                          var file = fileInput.files[0];
                          var textType = /text.*/;

                          var reader = new FileReader();
                          reader.readAsText(file);  
                          reader.onload = function(e) {
                                var x = reader.result.split("\r\n")
                                $(".trans_output").prepend("<h4 class='text-center'>Here is the output: </h4>" + 
                                      "<br><li><strong>Company Name: </strong></li>" + x[6].split("-")[1] + 
                                      "<li><strong>Ticker: </strong></li>" + x[6].split("-")[0] +
                                      "<li><strong>Event: </strong></li>" + x[7] +
                                      "<li><strong>Date: </strong></li>" + x[8]
                                );
                          }  
                    });

                $(".hide-code").click(function() {
                    $('.toggle').slideToggle()
                    $('.code_display').css('display','initial')
                })
            </xmp>
         </pre>
    </div>
</div>